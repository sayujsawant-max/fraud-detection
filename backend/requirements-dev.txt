# Backend dev + test dependencies.
# Pinned to floors that match the CI matrix; let pip resolve the rest.

pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0
httpx>=0.27.0

# Lint + format. Pinned exactly to the pre-commit ruff hook version so
# ``ruff check`` locally and in the hook produce the same output.
ruff==0.5.7

# Optional: pre-commit is a project-wide tool; we keep it in the backend
# dev requirements so ``make setup`` installs it in one shot.
pre-commit>=3.7.0
