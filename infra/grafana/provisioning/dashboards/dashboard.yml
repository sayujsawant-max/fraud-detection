apiVersion: 1

providers:
  - name: FraudShield
    orgId: 1
    folder: FraudShield
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
